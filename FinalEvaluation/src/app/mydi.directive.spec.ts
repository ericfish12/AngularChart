import { MydiDirective } from './mydi.directive';

describe('MydiDirective', () => {
  it('should create an instance', () => {
    const directive = new MydiDirective();
    expect(directive).toBeTruthy();
  });
});
